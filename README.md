# Event Rest API for BrainHub
> Rest API implemented in node.js + MongoDB

Insert an event, get all event, get an event by id and delete an event.

## Launch the server

After getting the source code, you should launch these commands:

npm install
npm start